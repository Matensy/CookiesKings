"""Launcher for the Session Injector desktop GUI.

Run this file directly (in PyCharm: right-click -> Run 'run_gui', or the green
▶ button). It lives at the project root and uses an absolute import, so it
works without the "run as module" dance.
"""
from SessionInjector.ui.gui import main

if __name__ == "__main__":
    main()
